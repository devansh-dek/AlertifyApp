<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Alertify" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="SuperbButtonStyle" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:backgroundTint">@color/superb_button_color</item>
        <item name="android:textColor">@color/white</item>
        <item name="strokeColor">@color/superb_button_color</item>
        <item name="strokeWidth">2dp</item>
        <item name="android:elevation">4dp</item>
        <item name="android:stateListAnimator">@null</item> <!-- Remove default button animation -->
    </style>
    <style name="Theme.GeminiAiChatbot" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.Alertify" parent="Base.Theme.Alertify" />
</resources>